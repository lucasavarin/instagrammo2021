package com.example.instagrammo.views.add

import com.example.instagrammo.views.BaseActivity
import com.example.instagrammo.views.home.HomeFragment

class AddActivity : BaseActivity(){


    companion object {
        var addActivity : AddActivity = AddActivity()
    }
}