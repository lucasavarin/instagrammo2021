<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    tools:context="fragments.HomeFragment">

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/homeFollowerRecyclerView"
        android:layout_width="0dp"
        android:layout_height="80dp"
        tools:context = "fragments.HomeFragment"
        tools:listitem="@layout/story_item"
        android:orientation="horizontal"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <!--<androidx.recyclerview.widget.RecyclerView
        android:id="@+id/homePostListLayout"
        android:layout_width="0dp"
        android:layout_height="80dp"
        tools:context = "activities.fragments.HomeFragment"
        tools:listitem="@layout/story_item"   DA CAMBIARE
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toBottomOf="homeFollowerListLayout"/>-->

    <!--<TextView
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:text="HOME"/>-->

</androidx.constraintlayout.widget.ConstraintLayout>